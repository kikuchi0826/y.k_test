let selected_day = document.getElementById('selected_day');
let schedule_day = document.getElementById('schedule_day');

function change_day() {
    let selected_day_day_value = selected_day.value; // 変更点: 初期化を関数内で行う
    console.log(selected_day.value);
}
